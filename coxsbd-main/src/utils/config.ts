// src/utils/config.js (Only PUBLIC Variables remain here)

// Maps NEXT_PUBLIC_API_URL from .env.local to your existing variable name.
// This is safe to use in client components.
export const BASE_API_URL = process.env.NEXT_PUBLIC_API_URL; 

// Maps NEXT_PUBLIC_BASE_URL from .env.local to your existing variable name.
export const BASE_URL = process.env.NEXT_PUBLIC_BASE_URL;

